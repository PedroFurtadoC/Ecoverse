# Copie este arquivo para `.env` e preencha as variáveis.
# `.env` está no .gitignore — nunca commite chaves reais.

# Supabase — Auth e leaderboard (opcional; o jogo roda sem isso usando localStorage)
# Pegue no painel do projeto Supabase em Settings > API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
